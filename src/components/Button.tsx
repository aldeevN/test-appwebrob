import React from "react";
import styles from "../styles/button.module.scss";




const Button = ({

}) => {
    return (
        <button
        >

        </button>
    );
}

export default Button;
